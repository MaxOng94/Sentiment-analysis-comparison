# Sentiment-analysis-compariosn

A project that compares sentiment analysis approaches that ranges from lexicon-based, pre-trained neural network approach and neural network custom trained on own data.
